<% if (page.total > 1){ %>

<nav class="paginator">
    <div class="wrapper">

        <%- paginator({
            prev_text: __('prev'),
            next_text: __('next')
        }) %>

    </div>
</nav>

<% } %>
